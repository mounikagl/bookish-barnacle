#include <stdio.h>

int main()
{
    int eggs = 60;

    int dozens = eggs/12;
    // printf("Dozens= %d\n", dozens);
    if (dozens)
    {
        printf("Dozens= %d\n", dozens);
    }
    return 0;
}
